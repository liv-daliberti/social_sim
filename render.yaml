services:
  - type: web
    name: social-sim-forecaster
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: cd exp1_prospective/viewer && gunicorn app:app --bind 0.0.0.0:$PORT
