services:
  - type: web
    name: social-sim
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: cd elections && gunicorn server:app --bind 0.0.0.0:$PORT
